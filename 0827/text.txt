express <- node.js 대표 웹 프레임워크 중에 하나 (되게 간단하게 되어있다)
node기반 nest.js <- 스프링부트와 유사

(스프링 <- 외워야 할 것이 많다. 흐름, 기본으로 정의되어 있는 것들이 많다)

예전에는 IP만 있었다.
IP 주소로 통신을 하려고 하니 한 가지 밖에 안된다.
그래서 나는 여러가지를 하고 싶은데?

TCP/IP. IP를 TCP로 감싸서 여기에 포트를 부여

http 통신은 기본적으로 stateless
상태가 없다

요청을 보내고 응답 받으면 끝
티키 -> 타카

티키타카티키타카 -> 웹소켓으로 채팅서버 같은 것을 구현
로그인 구현하면 -> 그 로그인 상태를 저장을 http 통신만으로 할 수 없다 
(그럴 때 쿠키, jwt 이런 것들을 사용한다)

브라우저 -> 백엔드 -> 브라우저

